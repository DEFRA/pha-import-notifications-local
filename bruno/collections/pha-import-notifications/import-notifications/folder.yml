info:
  name: import-notifications
  type: folder
  seq: 2

request:
  headers:
    - name: Content-Type
      value: application/json
    - name: x-api-key
      value: "{{cdp_developer_api_key}}"
  auth:
    type: oauth2
    flow: client_credentials
    accessTokenUrl: "{{token_url}}/oauth2/token"
    credentials:
      clientId: "{{client_id}}"
      clientSecret: "{{client_secret}}"
      placement: body
    tokenConfig:
      id: credentials
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: true
      autoRefreshToken: false
